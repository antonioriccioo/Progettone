package zizzi.progettone.model;

public enum TipoCella {
    // Enum che definisce i tipi possibili di una cella.
// Serve per evitare di usare stringhe.
// Ogni cella può essere solo uno di questi valori.
}
